<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    android:padding="10dp"
    android:layout_marginTop="5dp"
    android:layout_marginBottom="5dp"
    android:layout_marginRight="16dp"
    android:layout_marginLeft="16dp"
    android:id="@+id/cardview_item_show">

    <LinearLayout
        style="@style/AppTheme.MatchMatch">

        <ImageView
            android:layout_width="100dp"
            android:layout_height="150dp"
            android:layout_marginTop="20dp"
            android:layout_marginLeft="15dp"
            android:id="@+id/img_show_list"/>

        <LinearLayout
            style="@style/AppTheme.MatchMatch"
            android:layout_margin="10dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/name_show_list"
                style="@style/AppTheme.MatchWrap"
                android:layout_marginTop="20dp"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="@android:color/black"
                android:text="@string/name_show"/>
            <TextView
                android:id="@+id/desc_show_list"
                style="@style/AppTheme.MatchWrap"
                android:layout_marginTop="15dp"
                android:textSize="12sp"
                android:maxLines="3"
                android:text="@string/desc_show"/>
        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>